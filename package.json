{
  "name": "apex-coding-agent",
  "version": "0.0.0",
  "private": true,
  "description": "Long-running coding agent orchestrator around Claude Code (SPEC v4.1.1)",
  "type": "module",
  "engines": {
    "node": ">=22 <23 || >=24 <25"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.test.json",
    "test": "npm run typecheck && vitest run && node scripts/check-architecture.mjs"
  },
  "dependencies": {
    "ajv": "^8.17.1"
  },
  "devDependencies": {
    "@types/node": "^22.20.1",
    "typescript": "^5.6.3",
    "vitest": "^2.1.9"
  }
}
