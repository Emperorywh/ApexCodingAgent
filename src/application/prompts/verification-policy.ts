/**
 * 可写 Agent 共用的验证收敛与资源边界。
 *
 * Execution 与 Final Review 都可能运行测试或临时服务；统一策略可以避免
 * 两套提示词逐渐漂移，也让恢复会话继续遵守同一组资源生命周期约束。
 */
export const VERIFICATION_POLICY = `验证收敛与资源边界：
- 先读取并遵守仓库内适用于当前路径的代理说明；若说明要求界面由用户手动验证，不得启动浏览器或开发服务器，把未自动执行的部分如实记录为风险或 not_run。
- 先定义覆盖验收条件的最小验证集合，优先复用仓库已有命令和一个可重复运行的验证入口，不为同一问题连续创建多份临时脚本。
- 确需本地服务或浏览器且仓库允许时，只能由一个有界验证入口统一拥有启动、就绪检查、验证与清理；不得把独立的开发服务器留在后台，清理必须位于同一控制流的 finally 路径。
- 等待异步条件时使用带截止时间的条件轮询，不使用数分钟固定 sleep。相同失败原则上只进行两轮有针对性的修正；仍无法证明时停止扩大验证范围，据实返回 remainingRisks、failed 或 replan_required。
- 返回结构化结果前删除本 Session 创建的临时验证产物，并确认本 Session 启动的本地服务已经结束。`;
